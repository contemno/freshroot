# /etc/dracut.conf.d/freshroot.conf
# Enable the freshroot dracut module, but technically not necessary because 
#  module-setup.sh check() always returns 0, which means freshroot's dracut 
#  module is always installed regardless if no other module depends on it.
# add_dracutmodules+=" freshroot "
