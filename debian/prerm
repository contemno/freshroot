#!/bin/bash
set -e

case "$1" in
    remove|purge)
        systemctl disable --now freshroot-update.timer 2>/dev/null || true
        systemctl disable machine-id-persist.service 2>/dev/null || true
        ;;
esac

#DEBHELPER#
